## Interpreter
https://ibm.github.io/prompt-declaration-language/

- Prompt declaration language (PDL) to specify and force a structured prompt 
- contains prompt, contract and control loop
- Spec text is an ordered list where each can either be text (string, block (calls model)),conditional loops, DEFs (read data), and data 
- integrated via `liteLLM` 
